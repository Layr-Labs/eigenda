version: '3'
services:
  anvil:
    image: ghcr.io/foundry-rs/foundry:v1.0.0
    ports:
      - 8547:8547
    networks:
      - eigenda-demo
    command: ["anvil --host 0.0.0.0 --port 8547"]
networks:
  eigenda-demo:
    name: eigenda-demo
